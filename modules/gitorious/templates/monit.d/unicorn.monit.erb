check process unicorn with pidfile <%= pids_dir %>/unicorn.pid
  start program = "<%= t_app_root %>/bin/unicorn -c config/unicorn.rb -D"
  stop program = "/bin/sh -c '/bin/kill `cat <%= pids_dir %>/unicorn.pid`'"
