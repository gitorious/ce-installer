check process puppet with pidfile /var/lib/puppet/run/agent.pid
  start program = "/usr/bin/puppet agent<% if @fqdn %> --fqdn <%= @fqdn %><% end %> --masterport 443 --server packages.gitorious.com" 
  stop program = "/bin/sh -c '/bin/kill `/bin/cat /var/lib/puppet/run/agent.pid`'"
